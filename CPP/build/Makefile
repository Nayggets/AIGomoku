CXX=g++
SRC_PATH = ../src
INCLUDES = -I ../Include/ -I /usr/local/include
BUILD_PATH = ./
BIN_PATH = $(BUILD_PATH)bin

BIN_NAME = AIGomoku
COMPILE_FLAGS = -std=c++2a -Wall -Wextra -g -O0
LIBS = 


executable:
	${CXX} ${COMPILE_FLAGS} ${SRC_PATH}/*.cpp ${INCLUDES} -o ${BIN_PATH}/${BIN_NAME}